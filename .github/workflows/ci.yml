name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Test (Go ${{ matrix.go }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go: ['1.21', '1.25'] # floor + current
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}
      - name: gofmt
        run: test -z "$(gofmt -l .)"
      - name: go vet
        run: go vet ./...
      - name: Test (race)
        run: go test ./... -race -count=1 -timeout 300s

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.13.2

  # The whole test suite doubles as an integration test: every test that
  # normally runs against miniredis also runs against a real Redis and a
  # real Valkey server (same wire protocol, same Lua engine).
  integration:
    name: Integration (Redis & Valkey)
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7
        ports:
          - 6379:6379
      valkey:
        image: valkey/valkey:8
        ports:
          - 6380:6379 # Valkey speaks the Redis protocol on its own 6379
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
      - name: Test against Redis 7
        env:
          DISTSYNC_TEST_REDIS_ADDR: localhost:6379
        run: go test ./... -race -count=1 -timeout 300s
      - name: Test against Valkey 8
        env:
          DISTSYNC_TEST_REDIS_ADDR: localhost:6380
        run: go test ./... -race -count=1 -timeout 300s

  coverage:
    name: Coverage (>=75%)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
      - name: Measure coverage
        run: |
          go test . -count=1 -coverprofile=coverage.out -timeout 300s
          go tool cover -func=coverage.out | tail -1
      - name: Enforce threshold
        run: |
          go tool cover -func=coverage.out | awk '$$1=="total:"{gsub("%","",$$3); if ($$3+0 < 75) { print "coverage " $$3 "% below threshold 75%"; exit 1 } print "coverage OK: " $$3 "%" }'
